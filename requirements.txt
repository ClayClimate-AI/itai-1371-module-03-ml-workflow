# Declared dependencies for the Module 3 ML Workflow lab (Subject) + gate tooling (System).
# Pinned ranges kept conservative; CI pins Python to 3.12.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3

# Gate / delivery tooling
pytest>=7.4
jupyter>=1.0
nbconvert>=7.0
